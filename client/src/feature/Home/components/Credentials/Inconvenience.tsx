const Inconvenience = () => {
    return (
        <div className="mt-4 text-xs/6 sm:text-sm/7 lg:text-lg/8">
            Sorry for the inconvenience — the backend runs on Render’s free tier
            and may take a moment to wake up. It seeds the data and triggers a
            login on startup, so the first load can be slow. Thanks for your
            patience and for visiting. 🙏
        </div>
    );
};

export default Inconvenience;
